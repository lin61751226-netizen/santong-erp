# 三通工程自動化管理系統 — 交接記錄

> 最後更新：2026-09-06
> 最新程式版本：`2af7a0a`（已推送 origin/main；後續文件更新另有 commit）

---

## 〇、維護規則

每一次程式、環境設定、資料庫 migration、LINE 設定、Rich Menu、部署或介面調整，都必須在完成前同步更新本交接記錄。每筆紀錄至少包含：

1. 日期與目的
2. 修改檔案或設定項目
3. 實際變更內容
4. 驗證方式與結果
5. 明確未變更範圍
6. 尚未完成、風險或後續待辦

---

## 一、專案概述

- **系統名稱**：三通工程自動化管理系統
- **定位**：以 LINE 為員工操作入口、資料庫為核心、管理後台為控制中心的內部自動化系統
- **正式站**：https://santong-erp.onrender.com/（Render free plan，新加坡機房）
- **本機目錄**：`C:\Users\lin61\Documents\三通`
- **Python 虛擬環境**：`C:\Users\lin61\Documents\三通\.venv`（不在 backend 內）

---

## 二、技術架構

| 層級 | 技術 |
|---|---|
| 後端框架 | FastAPI 0.116 |
| ORM | SQLModel 0.0.24（SQLAlchemy 2.0） |
| 正式資料庫 | PostgreSQL（Render 提供） |
| 本機資料庫 | SQLite（`backend/data/santong.db`） |
| 排程 | APScheduler 3.11 |
| LINE | Messaging API Webhook + Push Message + Rich Menu |
| 檔案儲存 | Google Drive（工作相片自動上傳） |
| 管理後台 | FastAPI Jinja2 模板（單頁 `index.html`） |
| 密碼雜湊 | PBKDF2-SHA256（200,000 次迭代，標準庫實作，無第三方套件） |
| Session | HMAC 簽名 cookie（`santong_session`，httponly + samesite=lax） |

---

## 三、專案目錄結構

```
三通/
├── app.py                          # uvicorn 啟動入口（根目錄）
├── .env                            # 本機環境變數（含 Google service account）
├── .env.example                    # 環境變數範本
├── render.yaml                     # Render 部署設定
├── requirements.txt                # 根目錄（僅 uvicorn）
├── README.md                       # 專案說明
├── 交接記錄.md                     # 本檔案
├── 三通app.txt                     # 早期需求與進度記錄（至 7/28）
├── backend/
│   ├── requirements.txt            # 主要套件清單
│   ├── .env                        # 本機環境變數（config.py 實際讀取此檔）
│   ├── data/
│   │   └── santong.db              # 本機 SQLite
│   ├── tests/
│   │   ├── test_line_workflows.py  # 7 項 LINE 流程測試
│   │   └── test_auth_flow.py       # 10 項登入權限測試
│   └── app/
│       ├── main.py                 # FastAPI 入口（lifespan: init_db → seed → scheduler）
│       ├── models.py               # 資料模型（Employee/Worksite/WorkAssignment 等）
│       ├── schemas.py              # Pydantic schemas（含 LoginRequest 等）
│       ├── deps.py                 # 認證依賴（session cookie 驗證 + 角色權限）
│       ├── core/
│       │   ├── config.py           # Settings（pydantic-settings）
│       │   ├── db.py               # init_db + 輕量 migration + session
│       │   └── security.py         # 密碼雜湊 + session token 簽名
│       ├── routes/
│       │   ├── admin.py            # 後台 API（prefix=/api）
│       │   ├── auth.py             # 登入/登出/改密碼（prefix=/api/auth）
│       │   ├── line_management.py  # LINE 管理 API
│       │   └── line_webhook.py     # LINE webhook（/api/line/webhook）
│       ├── services/
│       │   ├── bootstrap.py        # 種子資料（8 員工 + 10 工地）
│       │   ├── google_drive.py     # Google Drive 上傳
│       │   ├── hr.py               # 考勤/請假邏輯
│       │   ├── line.py             # LINE 訊息處理
│       │   ├── line_platform.py    # LINE API 呼叫
│       │   └── scheduler.py        # 每日 07:00 推播
│       ├── static/
│       │   ├── app.css             # 後台樣式
│       │   └── richmenus/          # Rich Menu 圖片
│       └── templates/
│           ├── index.html          # 後台單頁（含全部 JS）
│           └── account_link.html   # LINE 帳號綁定頁
└── deploy/cloudflared/             # Cloudflare Tunnel 設定
```

---

## 四、8/7 登入與權限開發（commit ff4072b）

### 4.1 三項核心決策

1. **統一預設密碼首次登入**：所有後台帳號首次用 `Santong@2026` 登入，登入後強制跳轉改密碼頁面，改完才能使用後台
2. **後台使用者只有 owner + admin**：可登入帳號為 BOSS001（owner）、ADMIN001（admin）、ADMIN002（admin），其餘角色（employee/site_manager/external）登入直接回 403
3. **登入失敗 3 次鎖定 15 分鐘**：連續錯 3 次後帳號鎖定，期間即使密碼正確也無法登入，鎖定期滿自動解除

### 4.2 後端修改清單（10 處）

| 檔案 | 修改內容 |
|---|---|
| `core/security.py`（新增） | PBKDF2-SHA256 密碼雜湊 + HMAC 簽名 session token，全程標準庫 |
| `core/config.py` | 新增 5 個設定：`default_password` / `login_fail_limit` / `login_lock_minutes` / `session_expire_minutes` / `session_secret_key` |
| `models.py` | Employee 新增 6 欄位：`password_hash`、`failed_login_count`、`locked_until`、`must_change_password`、`session_key`、`session_expires_at` |
| `core/db.py` | 輕量 migration（ALTER TABLE ADD COLUMN），對既有 SQLite/Postgres 安全升級 |
| `schemas.py` | 新增 LoginRequest / ChangePasswordRequest / PasswordResetRequest |
| `routes/auth.py`（新增） | 5 端點：login、logout、me、change-password、reset-password |
| `deps.py`（重寫） | 移除 X-Actor-Code 模擬身分，一律以簽名 session cookie 驗證；must_change_password 阻擋上移到 get_current_actor |
| `services/bootstrap.py` | owner/admin 無密碼時以預設密碼初始化並強制改密碼；已改過密碼的帳號不覆蓋；LINE 綁定不受影響 |
| `main.py` | 註冊 auth router |
| `.env.example` / `render.yaml` | 同步新增 5 個環境變數 |

### 4.3 前端修改清單（2 處）

| 檔案 | 修改內容 |
|---|---|
| `templates/index.html` | 移除「模擬身分切換」下拉 → 改為登入者資訊 chip + 登出按鈕；新增登入覆蓋層與強制改密碼彈窗；api() 移除 X-Actor-Code，改用同源 session cookie；401 自動跳回登入畫面 |
| `static/app.css` | 新增 auth-overlay / auth-card / auth-error / user-chip 樣式 |

### 4.4 認證機制說明

- **密碼儲存**：格式 `pbkdf2_sha256$200000$salt_hex$digest_hex`，不存明文
- **Session**：登入成功後產生 32-byte 隨機 token，HMAC-SHA256 簽名後存入 cookie `santong_session`；原始 token 存入 Employee.session_key，過期時間存入 session_expires_at
- **Cookie 屬性**：httponly（防 XSS 竊取）、samesite=lax（防 CSRF）、production 環境下 secure（僅 HTTPS）
- **Session 過期**：預設 480 分鐘（8 小時），可由 `SESSION_EXPIRE_MINUTES` 調整
- **簽名密鑰**：優先使用 `SESSION_SECRET_KEY`，fallback 為 `LINE_CHANNEL_SECRET`，最後 fallback 為開發預設值（正式環境務必在 Render 控制台設定強隨機值）

---

## 五、可登入帳號清單

| 員工代碼 | 姓名 | 角色 | 綁定碼 | Email | 預設密碼 |
|---|---|---|---|---|---|
| BOSS001 | 三通工程行林老闆 | owner（經營者） | ST-1001 | a0900262580@gmail.com | Santong@2026 |
| ADMIN001 | 林金谷 | admin（系統管理者） | ST-1002 | lin61751226@gmail.com | Santong@2026 |
| ADMIN002 | 秀蓉 | admin（行政人員） | ST-1004 | linxiaoniu4@gmail.com | Santong@2026 |

> 其餘 5 人（BOT001 系統帳號、EMP001 勝忠、EMP002 小咪、EMP003 建成、EMP004 林小咪）不可登入後台，僅能透過 LINE 操作。

---

## 六、環境變數

### 6.1 完整清單

| 變數 | 預設值 | 說明 |
|---|---|---|
| `APP_NAME` | 三通工程自動化管理系統 | 系統顯示名稱 |
| `ENVIRONMENT` | development | production 時 cookie 啟用 secure |
| `DATABASE_URL` | sqlite:///./data/santong.db | 正式站為 PostgreSQL |
| `DEFAULT_ACTOR_CODE` | ADMIN001 | （保留，登入制後不再用於模擬身分） |
| `PUBLIC_BASE_URL` | http://127.0.0.1:8000 | 對外 URL（LINE webhook 用） |
| `TIMEZONE` | Asia/Taipei | 時區 |
| `LINE_CHANNEL_SECRET` | （空） | LINE 頻道密鑰 |
| `LINE_CHANNEL_ACCESS_TOKEN` | （空） | LINE 存取權杖 |
| `GOOGLE_SERVICE_ACCOUNT_JSON` | （空） | Google service account JSON 路徑或內容 |
| `GOOGLE_DRIVE_WORKLOG_FOLDER_ID` | 1j82gzF2... | 工作相片資料夾 ID |
| `GOOGLE_DRIVE_PUBLIC_SHARE` | true | 上傳後是否公開分享 |
| `DAILY_PUSH_HOUR` | 7 | 每日推播小時 |
| `DAILY_PUSH_MINUTE` | 0 | 每日推播分鐘 |
| **`DEFAULT_PASSWORD`** | **Santong@2026** | **後台統一預設密碼** |
| **`LOGIN_FAIL_LIMIT`** | **3** | **登入失敗鎖定次數** |
| **`LOGIN_LOCK_MINUTES`** | **15** | **鎖定持續分鐘數** |
| **`SESSION_EXPIRE_MINUTES`** | **480** | **Session 過期分鐘數** |
| **`SESSION_SECRET_KEY`** | （空） | **Session 簽名密鑰（正式站建議設定強隨機值）** |

### 6.2 本機與正式站差異

- **本機**：`backend/.env` 為 config.py 實際讀取的檔案（ROOT_DIR = backend）
- **正式站 Render**：環境變數由 render.yaml + Render 控制台注入；`SESSION_SECRET_KEY` 與 `DATABASE_URL` 為 `sync: false`，需在控制台手動設定

---

## 七、API 端點清單

### 7.1 認證（/api/auth，不需登入或自行驗證 cookie）

| 方法 | 路徑 | 說明 |
|---|---|---|
| POST | `/api/auth/login` | 登入（employee_code + password），回傳 must_change_password 旗標 |
| POST | `/api/auth/logout` | 登出（清除 cookie + DB session_key） |
| GET | `/api/auth/me` | 取得目前登入者資訊 |
| POST | `/api/auth/change-password` | 改密碼（current_password + new_password，至少 8 字元） |
| POST | `/api/auth/reset-password` | 管理者重設他人密碼為預設密碼（**目前未要求登入，作為緊急復原工具**） |

### 7.2 後台（/api，全部需登入 + owner/admin 角色）

| 方法 | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/meta/options` | 已登入 | 取得目前操作者、員工清單、工地清單 |
| GET | `/api/dashboard` | 已登入 | 系統總覽統計 |
| GET | `/api/employees` | 已登入 | 員工名冊 |
| PUT | `/api/employees/{code}` | owner/admin | 修改員工資料 |
| GET/POST | `/api/assignments` | 已登入 / owner,admin,site_manager | 工作安排查詢/建立 |
| POST | `/api/notifications/send` | owner,admin,site_manager | 發送臨時通知 |
| GET/POST | `/api/leave-requests` | 已登入 | 請假查詢/申請 |
| POST | `/api/leave-requests/{id}/decision` | owner,admin,site_manager | 核准/退回請假 |
| GET | `/api/leave-requests/{id}/reassignment-suggestions` | owner,admin,site_manager | 候補建議 |
| POST | `/api/assignments/{id}/apply-reassignment` | owner,admin,site_manager | 套用候補 |
| GET | `/api/attendance` | 已登入 | 考勤查詢 |
| GET | `/api/attendance/exceptions` | 已登入 | 考勤異常 |
| GET | `/api/me/schedule` | 已登入 | 我的行程 |
| GET | `/api/me/attendance` | 已登入 | 我的打卡 |
| GET | `/api/me/leaves` | 已登入 | 我的請假 |
| POST | `/api/meetings` | owner,admin,site_manager | 建立會議記錄 |
| POST | `/api/simulate/line-message` | owner,admin,site_manager | 模擬 LINE 訊息 |

### 7.3 LINE 管理

| 方法 | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/line-management/status` | owner,admin,site_manager | LINE 狀態 |
| POST | `/api/line-management/webhook/configure` | owner,admin | 設定 webhook URL |
| POST | `/api/line-management/webhook/test` | owner,admin | 測試 webhook |
| POST | `/api/line-management/richmenu/deploy` | owner,admin | 部署 Rich Menu |
| POST | `/api/line/webhook` | （LINE 驗簽） | LINE 訊息入口 |

---

## 八、測試結果

### 8.1 單元測試（17 passed, 0 failed）

執行方式（必須在 backend 目錄）：
```powershell
cd "C:\Users\lin61\Documents\三通\backend"
& "C:\Users\lin61\Documents\三通\.venv\Scripts\python.exe" -m pytest tests -q
```

- `test_line_workflows.py`：7 項（LINE 綁定、打卡、請假、群組、Rich Menu）
- `test_auth_flow.py`：10 項（登入成功、未登入 401、強制改密碼阻擋、改密碼後存取、3 次鎖定、非 owner/admin 拒登入、舊密碼失效、登出失效、過期 session、重設密碼）

### 8.2 本機流程驗證（13 項全通過）

未登入 401 → 錯密碼 3 次鎖定（401/401/423）→ 鎖定期間正確密碼也 423 → reset-password 解除 → 預設密碼登入 200 + must_change_password=true → 未改密碼訪問 dashboard 403 → 改密碼 200 → 改密碼後 dashboard 200 → 登出 200 → 登出後 401 → 員工角色 403 → 不存在帳號 401 → 首頁 HTML 含登入層

### 8.3 正式站線上驗證

- 首頁含登入層、無殘留 actorSelect
- ADMIN001 / BOSS001 皆可用預設密碼登入並正確觸發強制改密碼
- 登入後未改密碼訪問 dashboard 回 403
- 登出後訪問回 401

---

## 九、本機開發指令

### 9.1 啟動伺服器

```powershell
cd "C:\Users\lin61\Documents\三通\backend"
& "C:\Users\lin61\Documents\三通\.venv\Scripts\python.exe" -m uvicorn app.main:app --host 127.0.0.1 --port 8000
```

或從根目錄：
```powershell
cd "C:\Users\lin61\Documents\三通"
& ".venv\Scripts\python.exe" -m uvicorn app.main:app --app-dir backend --port 8000
```

### 9.2 執行測試

```powershell
cd "C:\Users\lin61\Documents\三通\backend"
& "C:\Users\lin61\Documents\三通\.venv\Scripts\python.exe" -m pytest tests -q
```

### 9.3 部署

```powershell
cd "C:\Users\lin61\Documents\三通"
git add .
git commit -m "訊息"
git push origin main
```
推送後 Render 自動部署（free plan 約 2-4 分鐘）。

---

## 十、後續優化方向

### 10.1 安全性（建議優先）

- **`reset-password` 端點目前未要求登入**——正式環境有風險，建議加權限保護（限定 owner/admin 且需登入）
- 密碼規則強化：大小寫 + 數字 + 符號、禁止與帳號相同、可選 90 天強制更新
- 加入登入稽核日誌（成功/失敗/鎖定事件），方便事後追查
- 在 Render 控制台設定強隨機 `SESSION_SECRET_KEY` 並定期輪換

### 10.2 權限與管理面

- 後台使用者管理介面：目前新增/停用帳號、重設密碼只能靠 API，建議做成 UI 按鈕
- owner / admin 權限差異化（如只有 owner 能管帳號）
- 鎖定清單 UI：顯示被鎖定帳號與剩餘時間，管理者可一鍵解除

### 10.3 使用體驗

- 登入頁目前是覆蓋層，可升級為獨立登入頁（更正式）
- 顯示「上次登入時間 / IP」
- 改密碼成功後提示「下次請使用新密碼登入」

### 10.4 部署與維運

- 正式環境確認 `secure cookie`（HTTPS）已生效
- 後台登入（密碼制）與 LINE 帳號綁定（bind_token 制）為兩套機制，建議文件化對應關係
- 定期備份 PostgreSQL

---

## 十一、已知限制與注意事項

1. **Render free plan**：閒置 15 分鐘後會休眠，首次請求需喚醒（約 10-30 秒）
2. **LINE 綁定保護**：任何 migration/seed 不得清除 line_user_id；_upsert_employee 的 preserved_line_user_id 邏輯必須保留
3. **密碼已改過的帳號不會被 seed 覆蓋**：bootstrap 只對 `password_hash IS NULL` 的 owner/admin 設預設密碼
4. **正式站 Postgres migration**：db.py 用 ALTER TABLE ADD COLUMN，對既有資料表安全；BOOLEAN DEFAULT 0 在 Postgres 會轉為 false
5. **本機 .env 編碼**：根目錄 `.env` 與 `backend/.env` 都存在，config.py 實際讀 `backend/.env`（ROOT_DIR = backend）
6. **requirements.txt 未新增套件**：安全模組全程用 Python 標準庫（hashlib/hmac/secrets），無 Render 安裝風險

---

## 十二、歷史進度摘要

| 日期 | 事件 | commit |
|---|---|---|
| 7/26 | 第一版骨架上線（LINE 入口 + 後台 + 6 項功能） | — |
| 7/28 | 四項問題修復：Rich Menu 字體放大、打卡上下班、請假六類別、群組不亂回覆；綁定保護機制 | bd04d7b |
| 7/28 | Rich Menu 安全切換別名（LINE 不允許覆蓋舊圖片） | 96a67b6 |
| 8/7 | 後台正式登入與權限：統一預設密碼 + 強制改密碼 + owner/admin 範圍 + 3 次鎖定 | **ff4072b** |

---

## 十三、Google Drive OAuth 2.0 認證改造（2026-09-02）

### 13.1 問題背景

員工透過 LINE 上傳的工作相片從 2026-07-27 起不再自動儲存到 Google Drive。檢查發現：
- 2026-07-13 ~ 07-26 期間正常上傳超過 231 張相片
- 2026-07-27 建立了空的日期資料夾，之後完全沒有新資料夾
- service account 可建立資料夾（不佔空間），但上傳實際檔案時返回 403 錯誤：Service Accounts do not have storage quota

### 13.2 根因

Google 在 2026 年 7 月底收紧了 service account 儲存政策：service account 本身沒有儲存配額，無法上傳檔案到個人 Google Drive。可建立資料夾（中繼資料，不佔空間），但無法上傳實際檔案內容。

### 13.3 解決方案

改用 **OAuth 2.0 使用者認證（refresh token）**，以 lin61751226@gmail.com 的身份上傳，使用該使用者的儲存空間。service account 保留為備援（僅供讀取，無法上傳）。

### 13.4 程式碼修改清單

| 檔案 | 修改內容 |
|---|---|
| pp/core/config.py | 新增 3 個設定：google_oauth_client_id / google_oauth_client_secret / google_oauth_refresh_token |
| pp/services/google_drive.py | 重寫認證邏輯：優先使用 OAuth 2.0 refresh token（OAuthCredentials），service account 作為備援；is_configured() 同時檢查兩種認證方式 |
| ackend/requirements.txt | 新增 google-auth-oauthlib>=1.0.0（用於 OAuth 授權流程） |
| .env.example | 新增 3 個 OAuth 環境變數範例 |
| 
ender.yaml | 新增 3 個 OAuth 環境變數（均 sync: false，需在 Render 控制台手動設定） |
| ackend/oauth_setup.py（新增） | OAuth 授權流程腳本，接受 --client-id / --client-secret 參數，跑 InstalledAppFlow 取得 refresh token |

### 13.5 新增環境變數

| 變數 | 說明 |
|---|---|
| GOOGLE_OAUTH_CLIENT_ID | OAuth 2.0 用戶端 ID（Google Cloud Console 建立） |
| GOOGLE_OAUTH_CLIENT_SECRET | OAuth 2.0 用戶端密鑰 |
| GOOGLE_OAUTH_REFRESH_TOKEN | OAuth 2.0 refresh token（透過授權流程取得，長期有效） |

> 注意：GOOGLE_DRIVE_WORKLOG_FOLDER_ID 已存在，不需重複設定。

### 13.6 OAuth 應用程式設定

- **Google Cloud 專案**：My Project 96206（watchful-bonus-251604）
- **應用程式名稱**：三通工程行
- **應用程式類型**：電腦版應用程式（桌面應用程式）
- **發布狀態**：測試中（Testing）
- **測試使用者**：lin61751226@gmail.com（已加入）
- **已啟用 API**：Google Drive API
- **用戶端 ID**：400874927345-sdv4u3q4sdbern3pn1km7rvcrh67ntq9.apps.googleusercontent.com

### 13.7 部署與驗證

- **本機驗證**：OAuth 2.0 認證成功、access token 取得成功、Google Drive 存取成功、測試檔案上傳成功（上傳到 2026-09-02_oauth_test 資料夾）
- **正式站部署**：2026-09-02 18:11 透過 Render 控制台手動部署，部署 ID dep-dabvchifngtc73fe55h0，46.3 秒完成，狀態「Deploy succeeded | Live」
- **正式站健康檢查**：/health 返回 200

### 13.8 注意事項

1. **refresh token 長期有效**，但如果使用者撤銷授權或更改密碼，token 會失效，需重新跑授權流程
2. **OAuth 應用程式處於測試中狀態**，只有測試使用者列表中的帳號可以授權；若要開放給其他使用者，需將應用程式發布為正式版（可能需要 Google 驗證）
3. **service account 金鑰仍保留**在本機和 Render 環境變數中，但僅供備援讀取，無法用於上傳
4. **Google Drive API 已在新專案啟用**，若未來更換專案，需重新啟用 API 並重新建立 OAuth 用戶端 ID

---

## 十四、歷史進度更新

| 日期 | 事件 | commit |
|---|---|---|
| 9/2 | Google Drive 認證從 service account 改為 OAuth 2.0，解決相片無法上傳問題；正式站已部署驗證 | 未 commit（需手動推送） |
| 9/5 | 整合三通工程堆高機出租管理系統至後台（/forklift 頁面 + 14 個 API + 3 資料表），11 項驗證全數通過 | ecedb61 |
| 9/6 | 堆高機每日點檢系統上線：6台車隊管理、LINE點檢流程、自動帶入工地、異常通知老闆、油量保養提醒、Rich Menu點檢按鈕、每日自動提醒、匯出CSV、狀態管理；停用EMP002小咪 | ece93a5 → 691d1bd → ab1b834 |

---

## 十五、堆高機出租管理系統整合（2026-09-05）

### 15.1 需求背景

將獨立的「三通工程堆高機出租管理系統」（純前端 SPA，資料存在瀏覽器記憶體）整合進現有後台，作為獨立 `/forklift` 頁面，資料改為後端 PostgreSQL 持久化，納入現有 session 登入保護。

- **源檔案**：`C:\Users\lin61\Documents\AI文件管理中心\備份\專案原檔\forklift-rental-system\`
- **原架構**：index.html + style.css + app.js（1652 行），7 模組純前端 SPA
- **整合方式**：保留原 SPA UI 與操作流程，資料層從 `appData` 記憶體改為 `fetch('/api/forklift/...')` 呼叫後端 API

### 15.2 已修改檔案

| 檔案 | 動作 | 說明 |
|---|---|---|
| `backend/app/models.py` | 修改 | 新增 ForkliftCustomer、ForkliftEquipment、ForkliftRental 三個資料表 |
| `backend/app/schemas.py` | 修改 | 新增 6 個 Pydantic 模型（Customer/Equipment/Rental 的 Create + Update） |
| `backend/app/routes/forklift.py` | 新增 | 客戶/設備/出租 CRUD + 儀表板統計 API，共 14 個端點 |
| `backend/app/main.py` | 修改 | 註冊 forklift_router，新增 `GET /forklift` 頁面路由 |
| `backend/app/templates/forklift.html` | 新增 | 堆高機管理頁面（側邊欄 6 模組導航 + modal 表單） |
| `backend/app/static/forklift.css` | 新增 | 複製原 SPA style.css（45KB），保留原視覺風格 |
| `backend/app/static/forklift.js` | 新增 | 改寫原 app.js，資料來源改為後端 API，新增登入檢查 |

### 15.3 資料表設計

**ForkliftCustomer（客戶）**
- `customer_code`：自動編號 C001、C002...
- `name`、`tax_id`、`contact`、`phone`、`email`
- `credit_limit`（信用額度）、`payment_terms`（付款條件）、`grade`（A/B/C 等級）

**ForkliftEquipment（設備）**
- `equipment_code`：自動編號 F001、F002...
- `name`、`brand`、`capacity`（噸數）、`fuel_type`（電動/柴油/汽油/LPG）
- `status`：可租 / 已租 / 維修中（字串，非 Enum）
- `daily_rate`（日租金）、`monthly_rate`（月租金）

**ForkliftRental（出租記錄）**
- `rental_code`：自動編號 R001、R002...
- `customer_id`、`equipment_id`（外鍵）
- `start_date`、`end_date`、`days`、`daily_rate`
- `total_amount`、`deposit`（押金）、`tax`（營業稅 5%）
- `status`：進行中 / 已結清 / 逾期

### 15.4 API 端點一覽

| 方法 | 路徑 | 說明 |
|---|---|---|
| GET | `/api/forklift/customers` | 客戶列表 |
| POST | `/api/forklift/customers` | 新增客戶（自動產生 C 編號） |
| PUT | `/api/forklift/customers/{id}` | 更新客戶 |
| DELETE | `/api/forklift/customers/{id}` | 刪除客戶（有出租記錄時回 400） |
| GET | `/api/forklift/equipment` | 設備列表 |
| POST | `/api/forklift/equipment` | 新增設備（自動產生 F 編號） |
| PUT | `/api/forklift/equipment/{id}` | 更新設備 |
| DELETE | `/api/forklift/equipment/{id}` | 刪除設備（有出租記錄時回 400） |
| GET | `/api/forklift/rentals` | 出租記錄列表（含客戶名稱、設備名稱） |
| POST | `/api/forklift/rentals` | 新增出租（自動產生 R 編號，設備自動標記已租） |
| PUT | `/api/forklift/rentals/{id}` | 更新出租記錄 |
| DELETE | `/api/forklift/rentals/{id}` | 刪除出租（設備自動恢復可租） |
| GET | `/api/forklift/stats` | 儀表板統計（總收入、押金、應收、設備使用率、狀態分布） |

全部端點依賴 `get_current_actor`，需登入且為 owner/admin 才能存取。

### 15.5 前端頁面功能

- **儀表板**：6 個統計卡片（總收入、押金、使用率、應收、出租筆數、客戶數）+ 2 個 Chart.js 圖表
- **客戶管理**：列表 + 新增/編輯/刪除 modal，含信用額度、付款條件、等級
- **設備管理**：列表 + 新增/編輯/刪除 modal，含狀態標籤（可租=綠、已租=橘、維修=紅）
- **出租管理**：列表 + 新增/編輯/刪除 modal，選擇設備後自動帶入日租金並計算總金額（含 5% 營業稅）
- **財務管理**：收入/稅/押金/應收 4 卡片 + 收入明細 + 出租記錄明細
- **報表功能**：客戶報表 / 出租報表 / 財務報表 三種，支援列印與 CSV 匯出
- 側邊欄有「返回工程後台」連結與登出按鈕
- 頁面載入時先呼叫 `/api/auth/me` 檢查登入，未登入或需改密碼時跳回 `/`

### 15.6 驗證結果

本機 TestClient 11 項驗證全數通過：

1. 未登入訪問 API 回 401
2. 登入成功，must_change_password=True
3. 未改密碼訪問回 403
4. 改密碼成功
5. 客戶 CRUD（新增 C001、列表、更新）
6. 設備 CRUD（新增 F001，status=可租）
7. 出租 CRUD（新增 R001）
8. 建立出租後設備自動變已租
9. stats 正確（income=11340, receivables=11340, utilization=100%）
10. 刪除出租後設備自動變回可租
11. 有出租記錄時不可刪除客戶/設備（400）
12. `/forklift` 頁面正常載入（含 css/js）

### 15.7 已知限制與後續優化方向

1. **工時管理模組未整合**：原 SPA 有 7 模組，本次整合保留 6 模組（移除工時管理，因與現有員工管理重疊）；未來可考慮複用現有 Employee 表建立工時記錄
2. **無資料匯入功能**：原 SPA 的 Excel 匯入未實作；未來可新增客戶/設備批次匯入
3. **無出租合約 PDF 產生**：目前只有報表預覽與 CSV 匯出；未來可整合 PDF 產生
4. **無逾期自動偵測**：出租記錄 status 需手動更新；未來可加排程自動將過期的「進行中」改為「逾期」
5. **無設備維修記錄**：設備可標記為「維修中」但無維修歷史；未來可新增維修記錄表
6. **無押金退還追蹤**：押金只有金額記錄，無退還狀態；未來可新增押金管理
7. **Chart.js 用 CDN**：正式站 Render 休眠喚醒時可能延遲；未來可改為本地 static 檔案


---

## 十六、堆高機每日點檢系統（2026-09-06）

### 16.1 需求背景

將堆高機管理整合進三通工程自動化管理系統，實作：
1. 車隊管理（1號、2號、3號、5號、6號、7號，共6台）
2. 員工透過 LINE 工作工具進行每日點檢（先選工地→選堆高機→逐項點檢）
3. 點檢異常自動通知老闆
4. 油量/保養自動提醒
5. 停用 EMP002 小咪帳號

### 16.2 三階段開發時程

| 階段 | commit | 內容 |
|---|---|---|
| 第一階段 | ece93a5 | 基礎架構：數據模型、6台堆高機初始化、LINE點檢流程、後台API、後台UI |
| 第二階段 | 691d1bd | 流程優化：自動帶入今日打卡工地、員工選今日開的堆高機、異常自動通知老闆、油量/保養提醒 |
| 第三階段 | ab1b834 | 5項優化：Rich Menu點檢按鈕、工作開始提醒、每日自動提醒、匯出CSV、狀態管理 |

### 16.3 新增資料模型

**ForkliftStatus（列舉）**
- `operating`：作業中
- `available`：可調度
- `maintenance`：待檢修
- `inactive`：停用

**Forklift（堆高機）**
- `forklift_code`：堆高機編號（1號、2號...）
- `model`：型號
- `status`：狀態（ForkliftStatus）
- `current_site_id`：目前工地
- `current_operator_id`：目前操作員
- `fuel_level`：油量（百分比）
- `next_maintenance_date`：下次保養日期

**ForkliftInspection（點檢記錄）**
- `forklift_id`：堆高機
- `operator_id`：操作員
- `site_id`：工地
- `inspection_date`：點檢日期
- `inspection_items`：10項點檢結果（JSON）
- `all_passed`：是否全部通過
- `notes`：備註

### 16.4 10項點檢項目

1. 引擎機油
2. 冷卻水
3. 電瓶液
4. 輪胎磨損
5. 喇叭
6. 燈光（前燈/尾燈/方向燈）
7. 剎車系統
8. 油壓系統
9. 貨叉與鏈條
10. 安全帶與後照鏡

### 16.5 6台堆高機初始化配置

| 堆高機 | 型號 | 預設工地 | 油量 |
|---|---|---|---|
| 1號 | 自排2.5噸柴油車 | 永森45 | 68% |
| 2號 | 手排2.5噸柴油車 | 齊裕53 | 92% |
| 3號 | 自排3.0噸柴油車 | 金駿76 | 74% |
| 5號 | 手排3.0噸柴油車 | 桃園28 | 54% |
| 6號 | 自排2.5噸柴油車 | 桃園29 | 80% |
| 7號 | 手排3.0噸柴油車 | 新竹寶山1 | 85% |

### 16.6 LINE 點檢完整流程

```
員工到達工地打卡
    ↓
點 Rich Menu「堆高機點檢」按鈕（或輸入「點檢」）
    ↓
自動帶入今日打卡工地（若無打卡記錄則顯示工地列表選擇）
    ↓
顯示所有6台堆高機，員工選今日開的車
    ↓
逐項點檢10項（每項回覆「正常」或「異常」）
    ↓
完成記錄，自動關聯操作員
    ↓
有異常 → 自動發 LINE 通知老闆（BOSS001）
油量低於30%或保養將至 → 自動提醒
```

### 16.7 5項優化說明

#### 1. Rich Menu 加入「堆高機點檢」按鈕
- 工作工具分頁從4個按鈕改為5個：到達工地、堆高機點檢、工作開始、工作完成、異常回報
- 員工不用打字，直接點按鈕開始點檢
- **需重新佈署 Rich Menu 才會生效**：後台「LINE 正式接線」→「佈署預設 Rich Menu」

#### 2. 工作開始前點檢提醒
- 員工輸入「工作開始」時，檢查今日是否已完成堆高機點檢
- 若尚未點檢，顯示提醒訊息（不強制阻擋，不開堆高機的員工可繼續）

#### 3. 每日自動提醒員工點檢
- 新增 scheduler 定時任務，每日早上7:30自動發 LINE 提醒
- 已完成點檢的員工不重複提醒
- 提醒內容包含點檢流程指引

#### 4. 點檢記錄匯出 CSV
- 新增 API：`GET /api/forklift-inspections/export`
- 支援依日期範圍、工地、堆高機篩選
- 匯出內容包含10項點檢結果明細
- 後台新增「匯出 CSV」按鈕

#### 5. 堆高機狀態管理
- 新增 API：`PUT /api/forklifts/{id}/status`
- 後台車隊管理表格新增狀態下拉選單
- 管理員（owner/admin/site_manager）可直接修改堆高機狀態
- 4種狀態：作業中/可調度/待檢修/停用

### 16.8 新增 API 端點

| 方法 | 路徑 | 權限 | 說明 |
|---|---|---|---|
| GET | `/api/forklifts` | 已登入 | 堆高機列表 |
| PUT | `/api/forklifts/{id}/status` | owner,admin,site_manager | 修改堆高機狀態 |
| GET | `/api/forklift-inspections` | 已登入 | 點檢記錄查詢 |
| GET | `/api/forklift-inspections/export` | 已登入 | 匯出點檢記錄 CSV |

### 16.9 已修改檔案清單

| 檔案 | 動作 | 說明 |
|---|---|---|
| `backend/app/models.py` | 修改 | 新增 ForkliftStatus、Forklift、ForkliftInspection 模型 |
| `backend/app/services/bootstrap.py` | 修改 | 移除 EMP002、新增6台堆高機初始化、管理員自動解鎖 |
| `backend/app/services/forklift_service.py` | 新增 | 點檢流程狀態管理、10項點檢定義、異常通知、油量保養提醒 |
| `backend/app/services/line.py` | 修改 | 整合點檢流程、工作開始前提醒、異常自動通知老闆 |
| `backend/app/services/line_platform.py` | 修改 | Rich Menu 加入「堆高機點檢」按鈕（5個按鈕佈局） |
| `backend/app/services/hr.py` | 修改 | 新增 get_today_arrival_site() 函數 |
| `backend/app/services/scheduler.py` | 修改 | 新增每日7:30自動提醒點檢定時任務 |
| `backend/app/routes/admin.py` | 修改 | 新增堆高機列表、狀態修改、點檢記錄查詢、匯出CSV API |
| `backend/app/templates/index.html` | 修改 | 新增堆高機車隊管理、點檢記錄查詢、狀態修改、匯出CSV UI |

### 16.10 帳號調整

- **EMP002 小咪**：已停用（inactive），從員工名冊移除
- **EMP004 → EMP005**：林小咪員工代碼調整
- **ST-1004 → ST-1003**：秀蓉綁定碼調整
- **BOT001 → ST-1009**：三通工程行line機器人綁定碼調整（避免衝突）

目前有效帳號：
- BOSS001 三通工程行林老闆 owner ST-1001
- ADMIN001 林金谷 admin ST-1002
- ADMIN002 秀蓉 admin ST-1003
- BOT001 三通工程行line機器人 external ST-1009
- EMP001 勝忠 employee ST-1005
- EMP003 建成Ray Rostova employee ST-1007
- EMP005 林小咪 employee ST-1008
- ~~EMP002 小咪 employee ST-1006~~（已停用）

### 16.11 部署與驗證

- **本機驗證**：所有 Python 檔案語法檢查通過
- **正式站部署**：3次 commit 均已推送，Render 自動部署完成
- **健康檢查**：/health 返回 200
- **後台 UI**：堆高機車隊管理、點檢記錄查詢區塊正常顯示

### 16.12 後續操作建議

1. **立即佈署 Rich Menu**：後台「LINE 正式接線」→「佈署預設 Rich Menu」，員工才能看到「堆高機點檢」按鈕
2. **測試完整流程**：員工到達工地打卡 → 點「堆高機點檢」→ 選擇堆高機 → 逐項點檢 → 完成
3. **查看明日7:30自動提醒**：確認 scheduler 正常運作
4. **測試匯出CSV**：在後台選擇日期，點「匯出 CSV」
5. **管理員測試狀態修改**：在後台車隊管理表格修改堆高機狀態

### 16.13 已知限制與後續優化方向

1. **點檢項目固定10項**：目前點檢項目寫死在程式碼中，未來可考慮做成可設定的資料表
2. **無點檢照片上傳**：目前點檢只有文字回覆，未來可考慮加入異常時上傳照片
3. **無月度統計報表**：目前只有CSV匯出，未來可考慮加入月度統計圖表
4. **Rich Menu 需手動佈署**：修改 Rich Menu 後需手動點「佈署預設 Rich Menu」才會生效
5. **點檢狀態存在記憶體**：點檢流程的中間狀態存在記憶體中，服務重啟後會遺失（不影響已完成的點檢記錄）

---

## 十七、後台操作介面優化（2026-09-06，已部署）

### 17.1 已調整內容

1. **五個工作分頁**：後台改以「今日總覽、派工與審核、堆高機管理、人員與考勤、LINE 與系統」切換，預設只顯示今日總覽，避免所有表單與表格堆在同一長頁。
2. **堆高機出租入口修正**：移除誤嵌入的外部交易網站，改為進入系統內 `/forklift` 出租管理頁；每日車隊狀態與點檢仍在工程後台查看。
3. **管理區塊結構修正**：使用者管理、工作相片上傳記錄、登入稽核日誌已移回主頁正確的 HTML 結構，並歸入「LINE 與系統」分頁。
4. **首次載入資料**：登入後同步載入車隊、今日點檢、使用者管理、相片記錄與登入日誌，切換分頁時不再出現尚未載入的空白內容。
5. **後端啟動修正**：`/api/forklifts/{id}/status` 的角色依賴改為正確參數格式，修復應用程式匯入時的 `TypeError`。

### 17.2 驗證

- `pytest tests -q`：17 passed。
- `compileall app -q`：通過。
- HTML 結構檢查：76 個唯一 ID、18 個工作分頁面板；已確認外部交易網站網址不存在，內部 `/forklift` 入口存在。

### 17.3 未變更範圍

- 未修改資料庫、LINE 綁定資料、Webhook、Rich Menu、角色權限規則與既有 API 路徑。
- 已推送 `d310e7e` 至 GitHub `main`，Render 正式站已確認含新版工作分頁與內部 `/forklift` 入口，健康檢查回傳 `200`。

---

## 十八、ADMIN001 登入復原（2026-09-06）

- 問題：ADMIN001 因登入失敗次數過多而鎖定，且原密碼遺失。
- 處理：以一次性啟動復原部署清除鎖定、失敗計數與舊登入工作階段，重設 ADMIN001 密碼並標記為首次登入必須修改密碼。
- 驗證：正式站登入 API 回傳 `200`、`employee_code=ADMIN001`、`must_change_password=true`。
- 收尾：驗證後已立即移除一次性復原程式，再次部署；未修改任何 `line_user_id`、LINE 綁定、其他帳號或資料庫資料。
- 相關版本：一次性復原為 `1768a53`；收尾移除復原程式為 `2af7a0a`，目前原始碼已不含自動覆蓋 ADMIN001 密碼的邏輯。
